# Project Guidelines

## To-Do

- [x] `2026-09-15` Implement AI checkpoint 36/43: reusable client recommendation state layer (no Dashboard rendering changes): `client/src/services/personalizedRecommendations.js` (constants `PERSONALIZED_RECOMMENDATION_PATH=/api/recommendations`, `DEFAULT_PERSONALIZED_RECOMMENDATION_LIMIT=10`, `MAX=100`, source `personalized-snapshot`, exact 35/43 disabled message, seven states `idle|loading|ready|no-snapshot|empty|disabled|error`, codes `RECOMMENDATION_REQUEST_FAILED|RECOMMENDATION_PAYLOAD_INVALID`, empty reasons `none|no-snapshot|empty-snapshot|feature-disabled|request-error`; `isValidRecommendationLimit` rejects bool/string/float/0/101/negative/NaN with no silent clamp; `buildPersonalizedRecommendationPath` emits only `?limit=<n>` — no userId/user_id/email/snapshot/artifact; `classifyPersonalizedRecommendationPayload` validates success envelope, source, status, contiguous integer ranks 1..N in received order without sort/repair, non-empty unique `song._id`, snapshot public 5-field projection with `available+unavailable===item_count` and `items.length<=available_count` and `items.length<=limit`, unknown status/wrong source/malformed → payload error; disabled ONLY exact `Personalized recommendations are currently unavailable.` (generic 503/network text → error); known safe messages preserved, otherwise fixed `failed to load personalized recommendations` (no stack/raw config); `fetchPersonalizedRecommendations` one GET via injected `apiClient.get`, invalid limit/missing client/throw → zero or single request no retry; `shouldUseLegacyRecommendationFallback` false for idle/loading/ready true for no-snapshot/empty/disabled/error; `getPersonalizedRecommendationEmptyReason` identifiers only): `client/src/hooks/usePersonalizedRecommendations.js` (`{limit=10,enabled=true}` → `{state,songs,snapshot,error,isLoading,isReady,shouldUseFallback,refresh}`; initial idle; enabled=false stays idle with no request; enabled mount idle→loading→one terminal; generation counter + cancelled flag for unmount/supersede (expected cancel never becomes error); no setInterval/setTimeout/auto-retry/polling; no PlayerContext/PlayHistory/ListeningEvent/trending/Dashboard import; reuses `api` from `client/src/api/client.js`); `client/src/pages/Dashboard/recommendationUi.js` pure re-export for future 37/43; Dashboard.jsx/.css/trendingUi/server/ml/package.json untouched; 88 new tests (75 service/hook-static + 13 UI); pass 88 new / 263 combined client tests (94 telemetry + 17 Admin helper + 61 Dashboard Trending + 3 formatDuration = 175 baseline) + 1666 backend + 1686 Python regressions, `compileall`, unchanged `runtime-info --json` twice byte-identical, `pip check`, and client production build.
- [x] `2026-09-15` Implement AI checkpoint 35/43: secure authenticated personalized recommendation API (`GET /api/recommendations` — `personalizedRecommendationService` + `recommendationRoutes` factory + `server.js` mount only): `protect` only (no `adminOnly`), feature flag `RECOMMENDATION_AI_ENABLED` / `recommendationConfig.aiEnabled` checked before parse/service with fixed 503 `Personalized recommendations are currently unavailable.` and zero snapshot/Song reads; strict limit-only query parser (default 10, max 100, unknown keys → 400); identity solely from `req.user._id`; one `getLatestRecommendationSnapshotForUser` read + one bulk `Song.find({_id:{$in}})` ≤100 IDs (no N+1); availability filter (`recommendation_eligible !== false` + playable `youtube_id|file_path` + non-empty `title`) BEFORE limit; snapshot order authoritative (no re-rank/fallback/popularity/random); status `ready|no-snapshot`, source `personalized-snapshot`; empty latest snapshot stays `ready` with zero counts; response exposes only `{status, source, items[{rank,song}], snapshot{snapshot_version, generated_at, item_count, available_count, unavailable_count}}` — no basis/scores/artifact/payload/user email; errors `PersonalizedRecommendationError|ValidationError|ReadError` with fixed message `failed to load personalized recommendations`; HTTP 503/400/401/200/500 map; read-only no writes/Python/SVD; 183 new tests (105 service + 78 route); pass 183 new / 1666 total backend tests + 94 telemetry + 17 Admin helper + 61 Dashboard Trending + 3 formatDuration + 1686 Python regressions, `compileall`, unchanged `runtime-info --json` twice byte-identical, `pip check`, and client production build.
- [x] `2026-09-15` Implement AI checkpoint 34/43: immutable personalized recommendation snapshot persistence (Node/Mongo contract only — no routes, no Python, no ranking/training/evaluation/artifacts; schema version 1 `RecommendationSnapshot` + `recommendationSnapshotService`): per-user caller-supplied `snapshot_version` `^[a-z0-9][a-z0-9._-]{0,63}$` with compound unique `{user, snapshot_version}` (same version allowed for different users), required `generated_at` (ISO-8601 with timezone or Date — never `Date.now()`), optional `artifact_version` same identifier policy or null, internal User ObjectId + ordered Song ObjectIds only (no title/artist/thumbnail/email/IP); items 0..100 with contiguous ranks 1..N, no duplicate Song IDs, exact basis vocabulary `hybrid|hybrid-profile|profile|exploration`, scores null or finite [0,1], structural basis/collaborative_known semantics without recomputing 0.80/0.20 or 0.70/0.30 formulas; summary 13 fields with conservation invariants (filter/candidate/selection/returned/items/profile_available/unknown-user seen=0/basis counts); empty snapshots valid; service-computed lowercase 64-hex SHA-256 over normalized payload (excludes `_id`/`createdAt`/`updatedAt`/`payload_sha256`) for idempotent retry identity; one create → on E11000-only one `findOne` (same hash → created:false, different hash → `RecommendationSnapshotConflictError`, missing → `RecommendationSnapshotPersistenceError`; no upsert/update/replace); `getLatestRecommendationSnapshotForUser` sort `generated_at:-1,_id:-1` `.lean()`, `getRecommendationSnapshotByVersion` exact lookup `.lean()`; model immutable (save/query/document-delete guards, `timestamps:true`, `versionKey:false`, `strict:true`, no Mixed, no TTL) with four indexes (unique user+version, user+generated_at+_id, version+generated_at, partial artifact+generated_at); no User/Song existence queries, no activation pointer, no route/server.js/auth/UI/Python changes; 283 new tests); pass 283 new / 1483 total backend tests + 94 telemetry + 17 Admin helper + 61 Dashboard Trending + 3 formatDuration + 1686 Python regressions.
- [x] `2026-09-15` Implement AI checkpoint 33/43: test-only deterministic recommender pipeline smoke coverage (`ml/tests/test_recommender_pipeline_smoke.py`, 49 tests; 3 temporal users + cold-start U4, 6 Songs, 27 events); train-only sparse input, held-out-only S4 content/policy handling, S6 exploration, test-partition relevance, actual policy-output evaluation, full-run repeatability and immutability; 1686 Python tests OK (3 platform symlink skips), 1200 backend + 175 client tests pass, compileall/runtime-info byte-repeat/pip check pass; production code and dependencies unchanged.
- [x] `2026-09-15` Implement AI checkpoint 32/43: immutable evaluation-run persistence (Node/Mongo contract only — no routes, no Python execution, no ranking/training/artifacts; schema version 1 `RecommendationEvaluationRun` + `recommendationEvaluationRunService`): caller-supplied safe run IDs `^[a-z0-9][a-z0-9._-]{0,63}$` (unique index, reject `.`, `..`, `/`, `\`, `:`, spaces, uppercase), stages `collaborative|hybrid|policy`, optional `artifact_version` same identifier policy or null, required `evaluated_at` (ISO-8601 with explicit timezone only — never `Date.now()`); ten 28/43 metrics each finite Number in [0,1] with no overall/combined quality score; summary (7 ints with `evaluated_user_count ≤ relevance_user_count`, `unique_recommended_at_10 ≤ catalog_size`, `diversity_evaluable_user_count ≤ evaluated_user_count`; `recommendation_user_count` may exceed evaluated); dataset (9 counts) conservation `raw == train + validation + test` (all-zero when raw==0, never silent correction); bounded configuration (`random_seed` required 0..4294967295, optional algorithm/components/weight pairs within 1e-9/`exploration_interval` 1..100, no silent renormalization); unknown keys rejected at top-level/metrics/summary/dataset/configuration; service-computed lowercase 64-hex SHA-256 `payload_sha256` over normalized payload (excludes `_id`/`createdAt`/`updatedAt`/`payload_sha256`) for idempotent retry identity — not auth/signature; one create → on E11000-only duplicate one `findOne` (same hash → created:false, different hash → `EvaluationRunConflictError`, missing → `EvaluationRunPersistenceError`; no upsert/update/replace); `listEvaluationRuns` options `limit` (1..100 default 20) + optional stage, sort `evaluated_at:-1,_id:-1`, `.lean()`, no metric/best sort, no cursor pagination; model immutable (`immutable:true` fields, `pre('save')` rejects non-new, query guards on update/delete ops, document `deleteOne` guard, `timestamps:true`, `versionKey:false`, `strict:true`, no Mixed, no TTL) with four indexes (unique run_id, evaluated_at, pipeline_stage+evaluated_at, partial artifact_version+evaluated_at); privacy: no user IDs/rec lists/raw ListeningEvents/Favorites/Playlists/profiles/emails/tokens/IPs persisted; forbidden tokens in model/service (no child_process/python/express/router/upsert:true/train/evaluate/winner/best/overall_score/quality_score); error classes `EvaluationRunValidationError`/`EvaluationRunConflictError`/`EvaluationRunPersistenceError`/`EvaluationRunImmutableError` under `EvaluationRunError`; new `server/services/recommendationEvaluationRunService.test.js`); pass 243 new / 1200 total backend tests + 94 telemetry + 17 Admin helper + 61 Dashboard Trending + 3 formatDuration + 1637 Python regressions, `compileall`, unchanged `runtime-info --json` twice byte-identical, and `pip check`.
- [x] `2026-09-15` Implement AI checkpoint 31/43: cold-start preference and deterministic exploration policy (`ml/recommender/cold_start.py` — `normalize_explicit_preference_profile(profile)` accepts `None`/`ExplicitPreferenceProfile`/plain mapping with only `favorite_song_ids` (list/tuple canonical 24-hex, dup reject), `playlist_song_counts` (1..`MAX_PLAYLIST_MEMBERSHIP_COUNT=250`), `artist_counts`/`genre_counts` (1..`MAX_PROFILE_FACTUAL_COUNT=1_000_000`, `normalize_content_value` keys, collapse-dup reject); caps `MAX_PROFILE_FAVORITE_SONGS=1000`/`MAX_PROFILE_PLAYLIST_SONGS=5000`/`MAX_PROFILE_ARTIST_ENTRIES=5000`/`MAX_PROFILE_GENRE_ENTRIES=5000` raise `ResourceLimitError` no truncation; frozen `ExplicitPreferenceProfile` (tuple + `MappingProxyType`); `rank_with_cold_start_policy(hybrid_context, user_id, candidate_song_ids, *, explicit_profile=None, limit=20)` validates `HybridRankerContext`, canonical user (unknown user is valid), list/tuple candidates ≤ `MAX_UNIQUE_SONGS` all in 26/43 content else `ColdStartValidationError`, limit 1..`MAX_POLICY_LIMIT=100`; filter order content membership → profile source Song IDs (favorites ∪ playlist keys) → known-user `observed>0` seen → collaborative-known vs cold-start Song split; known user calls `rank_hybrid_candidates` **exactly once** with remaining collaborative-known IDs and `limit=MAX_HYBRID_LIMIT`; unknown user **0 hybrid calls**, never fabricated collaborative scores; sparse profile vector from Favorite +1 unit + playlist count units over 26/43 features with **artist/genre family precedence** (non-empty direct aggregates replace source-Song artist/genre contributions; language/category only from source rows; out-of-vocabulary ignored; missing content row keeps ID in profile but skips vector); sparse cosine profile score in [0,1] or `None` (zero profile/candidate norm); policy formulas fixed baseline `BASE_HYBRID_POLICY_WEIGHT=0.80`/`EXPLICIT_PROFILE_POLICY_WEIGHT=0.20` sum 1.0 (not learned/optimized; no weight params on public API): both ⇒ `0.80*hybrid+0.20*profile` basis `hybrid-profile`, hybrid only ⇒ full hybrid basis `hybrid` (no ×0.80 penalty), profile only ⇒ full profile basis `profile`, neither ⇒ exploration-eligible only; exploration SHA-256 over `EXPLORATION_HASH_NAMESPACE="melodify-exploration-v1"|user|song` (no built-in `hash(`, no wall clock, no randomness), sort digest ASC → song_id ASC; cadence `EXPLORATION_INTERVAL=5` every-fifth-rank prefer exploration pool else exploitation pool, empty preferred pool backfills from other, no duplicates; exploration-selected items basis `exploration` with `policy_score=None` (hybrid/profile diagnostics retained); exploitation sort policy DESC → hybrid DESC (None last) → profile DESC (None last) → song_id ASC; contiguous 1-based ranks; frozen `ColdStartRankedItem`/`ColdStartRankingResult`/`ColdStartRankingSummary` factual counts only (no confidence/probability/rating/accuracy/quality); `ColdStartPolicyError`/`ColdStartValidationError` under `RecommenderRuntimeError`; pure Python no NumPy at import (no `configure_cpu_runtime` on import); no dense profile/matrix, no SVD/eval/artifacts/DB/HTTP/JS-service imports/popularity fallback; `ml/requirements.txt` unchanged; new `ml/tests/test_cold_start.py`); pass 308 new / 1637 total Python tests + 957 backend + 94 telemetry + 17 Admin helper + 61 Dashboard Trending + 3 formatDuration regressions, `compileall`, unchanged `runtime-info --json`, and `pip check`.
- [x] `2026-09-15` Implement AI checkpoint 30/43: deterministic known-user hybrid ranker (`ml/recommender/hybrid_ranker.py` — `prepare_hybrid_ranker()` validates exact model/bundle ID+map alignment and that every model Song exists in the 26/43 content bundle (extra content Songs allowed), builds the 29/43 training signal exactly once via `build_collaborative_training_signal()` with read-only CSR data/indices/indptr, precomputes one finite read-only float32 L2 norm per content row; `rank_hybrid_candidates(context, user_id, candidate_song_ids, *, limit=20)` for known users only; constants `COLLABORATIVE_WEIGHT=0.70`, `CONTENT_WEIGHT=0.30` (invariant sum 1.0; fixed initial baseline, not learned/optimized; public API accepts no weight/alpha/beta parameters), `DEFAULT_HYBRID_LIMIT=20`, `MAX_HYBRID_LIMIT=100`, `MAX_HYBRID_CANDIDATES=MAX_UNIQUE_SONGS`, `COLLABORATIVE_EQUAL_SCORE_VALUE=0.50`; list/tuple candidates only, canonical 24-hex, duplicates reject, cap overflow raises `ResourceLimitError` no truncation; seen filter uses only `observed>0` before the single `score_collaborative_candidates()` call (all-seen/empty ⇒ valid empty result, no scorer call, `seen_filtered_count` factual); candidate-set-local min-max of raw scores (equal/single ⇒ all 0.50 neutral; not probability/calibration); sparse content profile from positive training-signal entries × 26/43 features (dict accumulator, no dense profile vector, no Favorite/Playlist/18/43 sources); sparse cosine content score in [0,1], `None`+`content_available=False` when profile or candidate row norm is 0 (no fabricated similarity); hybrid = `0.70*collab_norm + 0.30*content` when content available else full `collab_norm` (no ×0.70 penalty); sort hybrid DESC → collab_norm DESC → collab_raw DESC → content DESC (None below numeric) → song_id ASC, then limit, contiguous 1-based ranks; frozen `HybridRankedItem`/`HybridRankingResult`/`HybridRankingSummary` (weights + factual counts, `content_available_candidate_count` before limit; no confidence/probability/rating/accuracy fields); `HybridRankingError`/`HybridValidationError`/`HybridColdStartUserError`/`HybridColdStartSongError` under `RecommenderRuntimeError` with bounded messages; lazy NumPy after `configure_cpu_runtime()` (import does not configure env or load numpy/scipy); no dense user×song/Song×Song, no cold-start fallback/exploration/random tie-break, no training/SVD/eval/artifacts/DB/HTTP/CLI changes; `ml/requirements.txt` unchanged; new `ml/tests/test_hybrid_ranker.py`); pass 216 new / 1329 total Python tests + 957 backend + 94 telemetry + 17 Admin helper + 61 Dashboard Trending + 3 formatDuration regressions, `compileall`, unchanged `runtime-info --json`, and `pip check`.
- [x] `2026-09-15` Implement AI checkpoint 29/43: deterministic collaborative latent-factor model (`ml/recommender/collaborative_model.py` — `build_collaborative_training_signal()` fixed heuristic CSR float32 formula `clip(1.00*observed + 0.50*log1p(session_count) + 0.25*log1p(play_started_count) + 1.50*log1p(completed_count) + 1.00*log1p(replay_started_count) + 0.25*log1p(listened_seconds/60.0) - 0.75*log1p(skipped_count), 0.0, 8.0)` with `stopped_count` neutral; weights are initial heuristic policy not learned/optimized; `train_collaborative_model()` fits `sklearn.decomposition.TruncatedSVD` (`algorithm="randomized"`, `n_iter=7`, `random_state=seed`, default/max factors 32, `effective=min(requested, users-1, songs-1)`); insufficient-data when users/songs/pairs < 2 or signal nnz=0 ⇒ `model=None` no fake factors; frozen `CollaborativeLatentModel`/`CollaborativeTrainingResult`/`CollaborativeTrainingSummary` with read-only float32 factors + `MappingProxyType` maps + schema version 1; `score_collaborative_candidates()` raw user·song dot products in input order, no ranking/sigmoid/clip/seen-filter/content; `CollaborativeUnknownUserError`/`CollaborativeUnknownSongError`; lazy NumPy/SciPy/sklearn after `configure_cpu_runtime()`; no dense user×song, no Favorite/Playlist/content/eval-tuning/artifacts/DB/HTTP/CLI train; `ml/requirements.txt` adds `scikit-learn>=1.5,<2` (1.9.1); new `ml/tests/test_collaborative_model.py`); pass 241 new / 1113 total Python tests + 957 backend + 94 telemetry + 17 Admin helper + 61 Dashboard Trending regressions, `compileall`, unchanged `runtime-info --json`, and `pip check`.
- [x] `2026-09-15` Implement AI checkpoint 28/43: deterministic recommender evaluation metrics (`ml/recommender/evaluation.py` — `evaluate_recommendations()` pure ranking metrics over caller-supplied ranked recommendations + held-out relevant Song IDs + catalog + 26/43 content bundle; constants `PRECISION_RECALL_KS=(5,10)`, `NDCG_KS=(5,10)`, `MAP_K=10`, `HIT_RATE_K=10`, `COVERAGE_K=10`, `DIVERSITY_K=10`, `MAX_RECOMMENDATIONS_PER_USER=100`; Precision@K denominator always K; binary NDCG@K with log2 rank discount; AP@K denominator `min(|relevant|,K)`; Hit@K; Catalog Coverage = unique top-10 recommended across evaluable users / catalog; intra-list Diversity = mean pairwise cosine dissimilarity over scorable top-10 pairs from sparse CSR rows only (zero-feature pairs excluded from denominator, never diversity=1); macro-average user metrics over evaluable users (non-empty relevance); missing recommendations = empty list; no evaluable users ⇒ all metrics 0.0 never NaN; canonical 24-hex IDs; `EvaluationError`/`EvaluationValidationError` under `RecommenderRuntimeError`; `ResourceLimitError` for caps; frozen `RecommenderEvaluationResult`/`RecommenderEvaluationSummary`; no overall/combined score; no model training/generation/SVD/DB/artifact/HTTP/event-based relevance inference; sparse only no `.toarray()`/`.todense()`; lazy NumPy/SciPy after `configure_cpu_runtime()`; `ml/requirements.txt` unchanged; new `ml/tests/test_evaluation.py`); pass 185 new / 872 total Python tests + 957 backend + 94 telemetry + 17 Admin helper + 61 Dashboard Trending regressions, `compileall`, unchanged `runtime-info --json`, and `pip check`.
- [x] `2026-09-15` Implement AI checkpoint 27/43: safe recommender artifact store (`ml/recommender/artifacts.py` — versioned immutable releases under `releases/<version>/` with SHA-256 `manifest.json` per file, staging + atomic `os.replace` publish, explicit activation writing schema-1 `current.json` pointer via temp+fsync+replace, `resolve_active_artifact_release` last-known-good verification with no auto-latest scan, JSON (deterministic sorted keys) + NPZ (`allow_pickle=False`, finite numeric dtypes only, ZIP path/size/entry inspection before load, no filesystem extraction) codecs, `ArtifactError` hierarchy under `RecommenderRuntimeError`, lazy NumPy after `configure_cpu_runtime()`, bounded limits (32 files / 64 MiB each / 128 MiB total / 64 metadata entries / 64 KiB metadata / 64 NPZ arrays / 256 MiB uncompressed / 128 ZIP entries); no pickle/joblib/eval/exec/model training/MongoDB/HTTP/cloud/durability claims; publication never auto-activates; `ml/artifacts/` gitignored; `ml/requirements.txt` unchanged; new `ml/tests/test_artifacts.py`); pass 198 new / 687 total Python tests + 957 backend + 94 telemetry + 17 Admin helper + 61 Dashboard Trending regressions, `compileall`, unchanged `runtime-info --json`, and `pip check`.
- [x] `2026-09-15` Implement AI checkpoint 26/43: deterministic sparse Song content-feature encoder (`ml/recommender/content_features.py` — `build_song_content_features()` encodes bounded Song metadata into one Song×Feature CSR `float32` matrix over exactly `("artist","genre","language","category")`; `normalize_content_value()` = NFKC+trim+whitespace collapse+casefold; `normalized_artist`/`normalized_genre` precedence with persisted fallback; no multi-value splitting/inference/title tokens; namespaces `artist::`/`genre::`/`language::`/`category::`; binary 1.0 only (no family weights/scores); sorted Song-ID rows + lexically sorted feature columns + `MappingProxyType` maps; zero-feature Songs retained as zero rows; `MAX_CONTENT_VALUE_LENGTH=512`, `MAX_CONTENT_FEATURES=MAX_UNIQUE_SONGS*4=100000`; lazy NumPy/SciPy after `configure_cpu_runtime()`; no dense/Song×Song/similarity/TF-IDF/SVD/model/DB/HTTP/artifacts/Favorite/Playlist/`recommendation_eligible` filtering; `ml/requirements.txt` unchanged); pass 162 new / 489 total Python tests + 957 backend + 94 telemetry + 17 Admin helper + 61 Dashboard Trending regressions, `compileall`, unchanged `runtime-info --json`, and `pip check`.
- [x] `2026-09-15` Implement AI checkpoint 25/43: bounded sparse interaction builder (`ml/recommender/sparse_interactions.py` — `build_sparse_interactions()` converts 24/43 `TemporalInteractionEvent` records into eight canonical CSR `float32` user×song factual matrices (`observed`, `session_count`, `play_started_count`, `replay_started_count`, `completed_count`, `skipped_count` positive count, `stopped_count`, `listened_seconds` stored-delta sum) with sorted ID indexes + `MappingProxyType` maps, frozen `SparseInteractionBundle`/`SparseInteractionSummary`, lazy NumPy/SciPy load after `configure_cpu_runtime()`, defensive field/session validation, 23/43 caps no truncation; `ml/requirements.txt` adds only `numpy>=1.26,<3` + `scipy>=1.12,<2`; no weights/scores/dense allocation/model/eval/artifacts/DB/HTTP/Favorite/Playlist; new `ml/tests/test_sparse_interactions.py`); pass 110 new / 327 total Python tests + 957 backend + 94 telemetry + 17 Admin helper + 61 Dashboard Trending regressions, `compileall`, unchanged `runtime-info --json`, and `pip check`.
- [x] `2026-09-15` Implement AI checkpoint 24/43: time-aware raw interaction split (`ml/recommender/temporal_split.py` — deterministic per-user session-level train/validation/test split; holdouts 1 validation + 1 test session, min 3 sessions to evaluate, sparse/overlap users train-only; server `createdAt` sole chronology, `client_occurred_at` ignored; no randomness/DB/file/HTTP/matrix/model; reuse 23/43 caps without truncation); pass 138 new / 217 total Python tests + 957 backend + 94 telemetry + 17 Admin helper + 61 Dashboard Trending regressions.
- [x] `2026-09-15` Implement AI checkpoint 23/43: bounded CPU-only Python recommender runtime foundation (`ml/` package — standard-library only `runtime.py` with frozen `RuntimeLimits`, hard safety ceilings seed 42 / 250000 raw events / 50000 users / 25000 songs / 1 worker / 1 numeric thread, idempotent process-local `configure_cpu_runtime()` env bootstrap, pure count/shape/seed validators with package-specific sanitized exceptions, argparse CLI `runtime-info` only; no training/model/MongoDB/HTTP/third-party ML); pass new unittest suites + 957 backend + 94 telemetry + 17 Admin helper + 61 Dashboard Trending regressions.
- [x] `2026-09-15` Implement AI checkpoint 22/43: Dashboard Trending Now integration (`Dashboard.jsx` + pure `trendingUi` helper — one authenticated `GET /api/trending?limit=10` per mount via existing API client, section order Recently Played → Trending Now → search/Recommended Songs, activity vs catalog-fallback cards without numeric score or fabricated fallback metrics, full filtered Song list to `player.playSong(list, index)`, quiet 503 hide, section-local empty/error, cancelled-mount guard, no Dashboard history/listening writes/polling/AI UI); pass new helper/static tests + 94 telemetry + 17 Admin helper + 957 backend regressions and client production build.
- [x] `2026-09-15` Implement AI checkpoint 21/43: Trending sparse-data fallback and ranking hardening (`trendingService` — activity-first with shared eligibility/playability predicate, deterministic catalog top-up `createdAt DESC _id ASC` when activity items < public limit, `basis`/`mode` vocabulary `activity|catalog-fallback`, fallback `score: null` + zeroed activity block, single bounded fallback query with 3×/150 lookahead and `$nin` ranked-ID exclusions max 100, activity always precedes fallback with contiguous ranks, empty catalog still HTTP 200, no randomness/popularity scoring/personalization/UI/route-change); pass 76 new / 957 total backend tests, 94 telemetry + 17 Admin helper regressions, and syntax checks.
- [x] `2026-09-15` Implement AI checkpoint 20/43: authenticated Trending API (`GET /api/trending` — protect + `RECOMMENDATION_TRENDING_ENABLED` gate before parse/service, strict limit-only query parser 1–50 default 10, bounded 7-day ListeningEvent query with MAX+1=50001 lookahead and oldest-row truncation to 50000, single engine call at candidate pool 100, single Song `$in` lookup with eligibility/playability/title/artist filters, engine-rank-preserving reconstruction with public limit last and renumber 1..N, empty list never 404, no user/Favorite/Playlist/personalization/fallback/UI); pass 103 new / 881 total backend tests, 94 telemetry + 17 Admin helper regressions, and syntax checks.
- [x] `2026-09-15` Implement AI checkpoint 19/43: transparent time-decayed Trending score engine (pure 7-day ListeningEvent window, 24h half-life decay, fixed event coefficients, per-user/song [-3,8] cap, unique-listener breadth, deterministic ranking; not AI/personalization/API/UI); pass 105 new / 778 total backend tests, 94 telemetry + 17 Admin helper regressions, and syntax checks.
- [x] `2026-09-15` Implement AI checkpoint 18/43: bounded factual user preference aggregation from windowed listening and current explicit evidence, with per-song/artist/genre/language totals and truncation; pass 155 new / 673 total backend tests, 94 telemetry + 17 Admin helper regressions, and syntax checks.
- [x] `2026-09-15` Implement AI checkpoint 17/43: bounded current Favorite/Playlist explicit evidence loader with trusted ID-to-email ownership resolution, deterministic deduplication and stale-song filtering; pass 70 new / 518 total backend tests, 94 telemetry + 17 Admin helper regressions, and syntax checks.
- [x] `2026-09-15` Implement AI checkpoint 16/43: manual next/previous/new-selection skips, confirmed same-session replay with continuing sequence, independent history after telemetry 503; pass 94 telemetry + 17 Admin helper + 448 backend regressions and client production build.
- [x] `2026-09-15` Implement AI checkpoint 15/43: client confirmed-playback telemetry (`listeningTelemetry` controller + `PlayerContext` integration — play-started/resumed only from confirmed YouTube PLAYING or HTML audio `play`, 15s throttled progress with seek-safe listened-delta ≤120s, pause/resume/seeked/completed, serialized per-session event queue with sequence 0 on first confirmed play, crypto-strong event/session IDs, auth-gated `POST /api/listening-events` via existing API client, 503 runtime disable with no retry/toast/blocking, PlayHistory centralized on confirmed start with Dashboard click-write removed; no skip/replay classification yet) and pass 53 client + 448 backend regression tests + client production build.
- [x] `2026-09-15` Implement AI checkpoint 14/43: authenticated `POST /api/listening-events` (`protect` only, no `adminOnly`, trusted `req.user._id`, `RECOMMENDATION_LISTENING_EVENTS_ENABLED` gate before parse/service, strict 13-field request whitelist rejecting unknown/user/token keys, one event per request, bounded HTTP status mapping, no PlayHistory/PlayerContext changes) and pass database-free regression tests.
- [x] `2026-09-15` Implement AI checkpoint 13/43: server-side `listeningEventService` recording layer (trusted userId, payload whitelist, Song existence, event-id idempotency/conflicts, sequence/session ordering with gaps allowed, bounded client-time + seek/pause-aware listened-delta checks, one E11000 recovery, sanitized fixed status/reason results; no route/PlayHistory/PlayerContext changes) and pass database-free regression tests.
- [x] `2026-09-15` Implement AI checkpoint 12/43: `ListeningEvent` Mongoose model for raw bounded playback evidence (User+Song ObjectIds, session/event IDs + sequence idempotency, fixed event/reason enums, capped position/duration/listened-delta, seek fields, optional client timestamp + playback source, four schema indexes, no TTL/derived scores) and pass schema-validation regression tests.
- [x] `2026-09-15` Implement AI checkpoint 11/43: Admin Music Catalog manual YouTube catalog-sync form (`CatalogSyncPanel` in existing music section, query/genre/language/maxResults 1–10, authenticated POST via existing API client, server-side feature-flag UX, bounded summary + per-item statuses, no auto-sync/API key/polling) and pass helper + backend regression tests + client production build.
- [x] `2026-09-15` Implement AI checkpoint 10/43: secure bounded admin catalog-sync API (`POST /api/admin/catalog-sync` behind `protect`+`adminOnly` and `RECOMMENDATION_CATALOG_SYNC_ENABLED`, one search + one details call, maxResults ≤ 10, 09/43 idempotent upsert, sanitized upstream errors, no pagination/background sync/UI) and pass database-free regression tests.
- [x] `2026-09-15` Implement AI checkpoint 09/43: idempotent YouTube catalog upsert service (canonical identity lookup/insert, single-match legacy adoption preserving `_id`, ambiguous/conflicting conflicts, import-managed refresh whitelist, E11000 single recovery, injected clock, no destructive merges) and pass database-free regression tests.
- [x] `2026-09-15` Implement AI checkpoint 08/43: pure YouTube music candidate normalizer (canonical identity, duration/thumbnail/status parsing, provisional topic-channel artist, conservative catalog eligibility, ordered search+details merge, no genre/language/mood inference) and pass database-free regression tests.
- [x] `2026-09-15` Implement AI checkpoint 07/43: server-side bounded YouTube catalog client (fixed host, injected fetch, 9s timeout, sanitized errors, no retries/pagination, no sync/routes/UI) and pass database-free regression tests.
- [x] `2026-09-15` Implement AI checkpoint 06/43: reject access tokens issued before `passwordChangedAt` with a generic 401, preserve 04/43 purpose separation and 05/43 reset security, and pass database-free regression tests.
- [x] `2026-09-15` Implement AI checkpoint 05/43: remove reset-token logging, sanitize reset-route and reset-sensitive global errors, preserve purpose separation, and pass database-free regression tests.
- [x] `2026-09-15` Implement AI checkpoint 04/43: separate access and password-reset JWT purposes, reject cross-purpose and missing-purpose tokens, and pass database-free security/regression tests.
- [x] `2026-09-15` Record standing permission to directly commit and push completed project changes to `aronno-is-here` using configured GitHub authentication.
- [x] `2026-09-15` Implement AI checkpoint 03/43: pure catalog identity helpers, safe partial Song identity/legacy lookup indexes, and database-free regression tests.
- [ ] `2026-09-15` Implement the reference-matched homepage on ehsanul in 50 numbered checkpoints; verify responsive rendering, preserve player architecture, merge without squashing, and verify the existing Vercel production deployment.
- [x] `2026-08-06` Add To-Do tracking system to AGENTS.md (track all user requests here)
- [x] `2026-08-06` Decide MERN vs keep PHP/HTML → **DECIDED: migrate to MERN** (Node 24 on G:, XAMPP on G:, karaoke already Node; CSS carries over verbatim so design is preserved)
- [x] `2026-08-06` Build Express + Mongoose backend (`server/`) replacing all PHP logic (auth, songs, uploads, playlists, admin)
- [x] `2026-08-06` Build React + Vite frontend (`client/`) preserving design exactly (CSS/JS split per page)
- [x] `2026-08-06` Port pages with per-page folders: Home, Login, Signup, Dashboard, Playlist, SongDetails, Premium, Profile, Admin, Karaoke (Playlist/SongDetails/Premium kept as static HTML+CSS+JS in `client/public/`, rest are React)
- [x] `2026-08-06` Split all mixed HTML/CSS/JS into separate files (one per language) — done as part of client port
- [x] `2026-08-06` Rename `Abon/`, `Choa/`, `Jannat/` folders → become `Playlist/`, `SongDetails/`, `Premium/` pages
- [x] `2026-08-06` Install MongoDB on G drive (installer saved in E drive) → **MongoDB 8.0.4 on `G:\MongoDB` (zip kept at `E:\Downloads\mongodb-windows-x86_64-8.0.4.zip`); DB seeded via `server/seed.js` from `all_data.sql` data**
- [x] `2026-08-06` Move `Posters/` + `songs/` into `assets/` and fix all paths (fix `Songs/` vs `songs/` bug)
- [x] `2026-08-06` Archive old PHP/HTML code in `legacy/`; sort whole project structure
- [x] `2026-08-06` Keep all `.exe` files in E drive — Mongo installer kept on `E:\Downloads`
- [x] `2026-08-06` Update README.md to match the new MERN structure
- [x] `2026-08-06` Split `karaoke-app/public/index.html` into `index.html` + `style.css` + `script.js` (byte-identical CSS/JS)
- [x] `2026-08-06` Commit MERN migration to git (`a5ba9d8`) — all updated files, archives, and docs
- [x] `2026-08-06` Rename leftover `legacy/Abon`, `legacy/Choa`, `legacy/Jannat` folders → `legacy/Playlist`, `legacy/SongDetails`, `legacy/Premium` (old page archives, best-matching names)
- [x] `2026-08-06` Add step-by-step "How to Run the Website" section to AGENTS.md (MongoDB → API → frontend → browser)
- [x] `2026-08-06` Fix homepage cut-off bug — Dashboard's global `html, body { overflow: hidden; height: 100% }` + Login/Signup/Admin `body { display:flex }` leaked onto all pages (all CSS loads globally); scoped with `body:has()` selectors (Dashboard: `main#mainContainer`, Login: `.login-container`, Admin: `.admin-login-page`), Signup wrapped in `.signup-page` div — superseded by per-page CSS isolation below
- [x] `2026-08-06` Restore exact original design on all pages — all page CSS loaded globally so later pages (Admin.css `.btn`, Dashboard.css `header`/`.logo`, Login/Signup/Profile `.logo`) overrode Home.css (logo/buttons wrong). Replaced static `import './X.css'` with per-page isolation: `import cssRaw from './X.css?raw'` + `useLayoutEffect` injecting `<style data-page-css="X">` into `<head>`, removed on unmount (`Home`, `Login`, `Signup`, `Dashboard`, `Profile`, `Admin`). Reverted the `body:has()` workarounds back to original `html, body` / `body` text. Admin body rule (flex-centering from `admin_login.html`) split so the panel uses normal flow via existing `.admin-login-page`. Aligned Dashboard.css to legacy `user_dashboard.html`. Verified headless via Edge: each route loads ONLY its own page CSS (`/` Home, `/login` Login, `/signup` Signup, `/dashboard` Dashboard, `/profile` Profile, `/admin` Admin); homepage buttons are black text on sky-blue with `padding 8px 20px`/`12px 30px`, header logo 28px left, auth links right, html scrollHeight 3131/overflow visible
- [x] `2026-08-06` Create `RUNNING_AND_ADMIN_GUIDE.md` — standalone doc covering manual run steps (MongoDB → API → frontend → browser, ports, first-time setup, troubleshooting) and full Admin Panel management (credentials, all 7 sections, resetting admin password)
- [x] `2026-08-06` Remove admin credentials from the GitHub repo — seed.js had hardcoded fallback `admin123` and README/guide/AGENTS.md documented it. Now: `ADMIN_PASSWORD` comes from gitignored `server/.env`; if missing, seed generates a random password printed once. Rotated the local admin password + JWT_SECRET in `.env` and re-hashed the admin user in MongoDB; verified `admin123` no longer works and the new password logs in. Docs updated (README, guide, AGENTS.md) with env-based instructions
- [x] `2026-08-06` Dashboard middle column: "Recently Played" horizontal slider (latest 20 played songs per user) added on top, then search, then "Songs" renamed "Recommended Songs". New `PlayHistory` model + `/api/history` routes (POST record, GET latest 20 populated). Seek bar replaced with a native `<input type="range">` styled like the volume slider (always renders) whose track is a `linear-gradient` filled sky-blue up to the thumb position via a `--fill` CSS var — verified via API + headless Edge
- [x] `2026-08-06` **All static pages converted to dynamic React** — no dummy content remains. Playlist/SongDetails/Premium moved from `client/public/` to `client/src/pages/` (designs byte-identical via `?raw` CSS injection). New shared `client/src/hooks/usePlayer.js` (YouTube IFrame + `<audio>` fallback, repeat/shuffle/seek/volume, auto-next, error skip-guard) powers the new pages. Backend: `GET /api/songs/:id`, `GET/PUT /api/playlists/:id`, Playlist model upgraded `songIds` → `items[{songId, addedAt}]` (populated), `Subscription` gains `plan` field + new `/api/subscriptions` routes (`GET /me`, `POST /`, `PUT /cancel`). Pages: **Playlist** (`/playlist/:id`, protected) — real per-user playlists, play-all, row play, remove song, rename, share, delete, live search-and-add from library, floating player; **SongDetails** (`/song/:id`, protected) — real metadata + related tracks (same artist → same genre → rest), footer player w/ shuffle/repeat/seek/volume; **Premium** (`/premium`) — live subscription status, subscribe/cancel Individual/Student/Duo, dynamic trial-end date, auth-aware nav, FAQ accordion. Entry points: song info click → `/song/:id`; Profile Premium link updated. The "My Playlists" button initially added to the Dashboard Library header was later removed at the user's request (playlists stay reachable via `/playlist/:id`; admin gets 2 seeded playlists). Old static pages archived to `legacy/static_pages/`. Verified: build passes, reseeded (2 playlists seeded for admin), API end-to-end (login → playlists populated → song detail → subscribe → /me active)
- [x] `2026-08-06` Dashboard player now uses shared `usePlayer.js` hook — previously used raw `<audio>` with `file_path` which couldn't play seeded YouTube songs; now supports YouTube IFrame API + audio fallback
- [x] `2026-08-06` Extract duplicate `escapeRegex` into `server/utils/escapeRegex.js` — removed from authRoutes, songRoutes, adminRoutes; imported from shared utility
- [x] `2026-08-06` Add pagination to `GET /api/songs` — supports `page` and `limit` query params, returns `total` and `pages` count
- [x] `2026-08-06` Remove non-functional Google OAuth stub buttons from Login and Signup pages
- [x] `2026-08-06` Install karaoke-app `node_modules` — `npm install` in `karaoke-app/server/`
- [x] `2026-08-06` Commit all MERN migration + fixes to git (`df89ce5`) — 52 files changed, includes all dynamic pages, shared player hook, shared utility, pagination, OAuth stub removal
- [x] `2026-08-06` Create `MONGODB_CONNECTION_GUIDE.md` — standalone doc covering: starting MongoDB, how the project connects via `MONGO_URI` in `server/.env`, seeding, connecting MongoDB Compass (step-by-step), what you can manage in the Compass GUI, and troubleshooting table
- [x] `2026-08-06` **YouTube streaming migration** — every seeded song now plays via the YouTube IFrame API instead of local MP3s. Added `youtube_id` to the Song model; seed now stores official YouTube IDs + official thumbnails (`https://img.youtube.com/vi/<ID>/hqdefault.jpg`) as `poster_url`; all 14 IDs verified by fetching each official video's title (e.g. Dukkho Bilash `ECh1rS2ipJw` G Series official, Nisshash `VHz3srJjAV4` Real Energy official, Die With A Smile `Fn6Ul6sYqro` official MV). Fixed seed metadata: Keno Hothat Tumi Ele artist `Romantic`→`Tahsan` + genre `Hindi`→`Bengali`; Nisshash artist `Borbaad`→`G.M. Ashraf` + genre `Rock`→`Bengali`. Dashboard player rewritten: hidden `#yt-player` div + `window.YT` script loader (`onYouTubeIframeAPIReady`), `loadVideoById`/`setVolume`/`seekTo`, 250ms `getCurrentTime` polling, `onStateChange` (PLAYING/PAUSED/ENDED→repeat or next), `onError`→next; shuffle/repeat/volume/mute/seek preserved; `<audio>` kept only as fallback for user-uploaded songs without `youtube_id`. Verified: `npm run build` passes, reseeded DB, API returns `youtube_id` + YouTube `poster_url` for all 14, thumbnails return HTTP 200
- [x] `2026-08-06` **All static pages converted to dynamic React** — no dummy content remains. Playlist/SongDetails/Premium moved from `client/public/` to `client/src/pages/` (designs byte-identical via `?raw` CSS injection). New shared `client/src/hooks/usePlayer.js` (YouTube IFrame + `<audio>` fallback, repeat/shuffle/seek/volume, auto-next, error skip-guard) powers the new pages. Backend: `GET /api/songs/:id`, `GET/PUT /api/playlists/:id`, Playlist model upgraded `songIds` → `items[{songId, addedAt}]` (populated), `Subscription` gains `plan` field + new `/api/subscriptions` routes (`GET /me`, `POST /`, `PUT /cancel`). Pages: **Playlist** (`/playlist/:id`, protected) — real per-user playlists, play-all, row play, remove song, rename, share, delete, live search-and-add from library, floating player; **SongDetails** (`/song/:id`, protected) — real metadata + related tracks (same artist → same genre → rest), footer player w/ shuffle/repeat/seek/volume; **Premium** (`/premium`) — live subscription status, subscribe/cancel Individual/Student/Duo, dynamic trial-end date, auth-aware nav, FAQ accordion. Entry points: song info click → `/song/:id`; Profile Premium link updated. The "My Playlists" button initially added to the Dashboard Library header was later removed at the user's request (playlists stay reachable via `/playlist/:id`; admin gets 2 seeded playlists). Old static pages archived to `legacy/static_pages/`. Verified: build passes, reseeded (2 playlists seeded for admin), API end-to-end (login → playlists populated → song detail → subscribe → /me active)
- [x] `2026-09-15` **Lyrics feature** — Song model with `lyrics` + `chords` fields, admin PATCH route, LyricsChordsPanel with synced lyrics highlighting, LRCLIB proxy
- [x] `2026-09-15` **Synced lyrics from LRCLIB** — LRC parsing, line-by-line highlighting, auto-scroll, fallback search by title+artist+duration
- [x] `2026-09-15` **Romanized Hindi/Bengali lyrics** — Pure JS transliteration for Devanagari and Bengali scripts; non-Latin lyrics automatically converted to romanized text (e.g., "कैसे बताएं" → "kaise bataen"); English lyrics unchanged. Production verified on Vercel for Hindi (Khamoshiyan), Bengali (Keno Hothat Tumi Ele), and English (Comfortably Numb)

## How to Run the Website (step by step)

Run all three services in this order (3 terminals). The Vite frontend proxies `/api` and `/assets` to `localhost:5000`.

### Step 1 — Start MongoDB (database)

```powershell
& "G:\MongoDB\bin\mongod.exe" --dbpath "G:\MongoDB\data" --logpath "G:\MongoDB\log\mongod.log" --logappend
```

- Runs on `localhost:27017`. Leave this window open.
- If MongoDB is not installed yet, extract `E:\Downloads\mongodb-windows-x86_64-8.0.4.zip` to `G:\MongoDB` and create the `data` and `log` folders.
- To reset/reseed the DB with the 14 songs + admin user:

```powershell
# in the server/ folder
npm run seed
```

### Step 2 — Start the API backend (Express)

```powershell
cd "D:\Aronno\Works\Melodify - Music Streaming Website\server"
npm install    # first time only
npm run dev
```

- Runs on `http://localhost:5000`. Leave this window open.
- Verify: open `http://localhost:5000/api/health` → should return `{"status":"ok"}`.

### Step 3 — Start the frontend (React + Vite)

```powershell
cd "D:\Aronno\Works\Melodify - Music Streaming Website\client"
npm install    # first time only
npm run dev
```

- Runs on `http://localhost:5173`. Leave this window open.

### Step 4 — Open the website

- Browse to **http://localhost:5173**
- Admin login: email + password come from `server/.env` (`ADMIN_EMAIL` / `ADMIN_PASSWORD`, never hardcoded in the repo) → visit http://localhost:5173/admin
- Normal users: sign up at http://localhost:5173/signup
- Dynamic pages: http://localhost:5173/playlist/:id (protected), http://localhost:5173/song/:id (protected), http://localhost:5173/premium (public)
- Karaoke app: open `karaoke-app/` separately (served from its own folder)

### Notes

- If a port is already in use, check `Get-NetTCPConnection -LocalPort <port> -State Listen` and kill the old process first.
- Server logs: `server/server.log`; Vite logs: `client/vite.log`.
- Media: seed songs stream from YouTube (see `youtube_id` in the Song model); local files under `assets/songs/uploads/` are only user uploads (gitignored — do not delete).

## To-Do Tracking

- Maintain a `## To-Do` list in this file (AGENTS.md).
- **Every time the user gives a new instruction or request, update the To-Do list in AGENTS.md** — add new tasks, mark completed items as `- [x]`, and keep statuses current.
- Each item must start with a timestamp in `YYYY-MM-DD` format: `- [ ] \`2026-08-06\` task description`.
- **Default timestamp**: when adding a new item, always use the most recent timestamp already present in the file unless the user provides a specific time.
- If the user gives a time (e.g. "do what was on the list for 2026-08-10"), use it to filter/follow only the items with that timestamp.
- Keep the list in sync with the actual project state.

## README Maintenance

- **Always update README.md whenever code or project structure changes.**
- Keep the Features, Project Structure, and Getting Started sections in sync with the actual code.
- Update the README in the same commit as the code change (or a commit immediately after).
- If a feature is removed or renamed, remove/update its README entry too.

## Default GitHub Workflow

- The user authorizes directly committing and pushing completed project changes to `aronno-is-here/melodify-music-streaming` without asking for confirmation each time, unless a later instruction overrides this preference.
- Use configured GitHub authentication for `aronno-is-here`; never store account passwords or tokens in project files or commit them.
- Push the active task branch to its intended remote branch after appropriate verification and review of the changes.

## GitHub Collaborator — Commit as Contributor

- When the user asks to "commit for mimi" or "commit for ehsanul", read the credentials from `.credentials.json` (gitignored).
- **Workflow:** read `.credentials.json` → set user config + remote with token → commit as collaborator → push → clean remote URL.
- The repo is `aronno-is-here/melodify-music-streaming` on GitHub.
- Branches: `mimi` (for mimibintesharif), `ehsanul` (for mmostaba21372-web), `choa` (for choaIslam).

### Branch Workflow for Contributors
1. **Switch to the contributor's branch** (`mimi`, `ehsanul`, or `choa`)
2. **Make the changes** on that branch
3. **Commit** with the contributor's author info
4. **Push** to their branch using their PAT
5. **Merge** the branch into `main` (fast-forward or merge commit)
6. **Push main** to origin
7. **Clean** the remote URL (remove token)

## Commit Policy — NEVER commit

- `.env` files (any location, any variant: `.env`, `.env.local`, `.env.*` — only `.env.example` allowed)
- API keys, tokens, or secrets (auth tokens, JWT secrets, OAuth client secrets, etc.)
- Database passwords / connection strings containing credentials
- AWS/GCP/Azure credentials or any cloud service-account keys
- Private certificates (`.pem`, `.crt`, `.key`, `.p12`, `.pfx`, `.p8`)
- SSH keys (`id_rsa`, `id_ed25519`, known_hosts, config with keys)
- Customer/personal data (PII, user data dumps)
- Paid datasets you don't have permission to share (e.g. the MP3 files in `assets/songs/` are gitignored for this reason)

**Before every commit:** run `git status` and review `git diff` to confirm no secrets or sensitive files are staged. If something sensitive is committed by mistake, rotate the secret immediately and rewrite history only with explicit user approval.
